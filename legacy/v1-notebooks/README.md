# ANTHEIA v1 notebooks (July 2026)

The notebook pipeline behind `README ANTHEIA2026.07.md` (now `docs/archive/`): existence matrices, PCA embeddings, PPE
integration, the five logistic-regression variants and the 5-seed evaluation. Kept for the record of the published v1
numbers only. They read absolute paths under `/scratch/ariana.l/` on crow, several cannot run as written (see
`docs/plan/` and the data-bug note in `docs/archive/`), and nothing in the package depends on them. New work uses
`antheia/` and `pipelines/`.
